# set up python virtual environment

# requirements.txt has all the extra requirement

`(venv) > pip install -r requirements.txt`

### run app.py

[flask app](http://localhost:5001/)

# setting up sql databas

`run http://localhost:5001/set_up`
